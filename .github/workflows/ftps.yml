name: FTPS Test

on:
  push:
    branches: [master]

jobs:
  deploy:
    name: 🚀 Deploy website every commit
    runs-on: ubuntu-latest
    steps:
      - name: 🚚 Get latest code
        uses: actions/checkout@v4

      - name: 📂 Sync files
        uses: ./
        with:
          server: ftptest.balajibn64.io
          username: Balajibn64
          password: ${{ secrets.FTP_PASSWORD }}
          protocol: ftps
